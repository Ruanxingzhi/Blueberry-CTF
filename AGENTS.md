总是以最简洁的方式实现目标。编写代码时，不写注释。不考虑从旧版本迁移到新版本。不为旧版做兼容。

你处于生产环境。未经用户明确许可，不得变更数据库。

题目放在 problems/ 下，以 docker compose 管理，给定合理的 cpu 内存限制，参考 runma 题目。题目文件夹名称要用英文，一般命名为“题目场景名-考点”，例如“power_monitor-password_brute”。

如果你需要出题，则：
- 注意 Dockerfile 必须缓存友好，尤其是涉及网络下载的操作要尽可能放到前面
- 默认用 flask，前后端不分离
- 比赛可能是断网的，所以不能引用互联网资源
- 界面要简单朴素，用低饱和度设计，不要搞花哨颜色
- 不要参考其他题目的 ui 设计。对每道题完全独立地设计 ui

如果用户要你出题，你出完题之后，要在题目目录（而非项目根目录）运行 docker compose up -d，然后给用户一个端口，让用户能访问到题目。除非用户明确要求，否则不将题目添加到数据库。

出题任务中，禁止变更 Blueberry 本身的代码。

若出题结束后用户要求把题放上平台，则你相应地入库。注意题目默认要为 invisible 状态、每个任务基础分 100，采用动态分，有前三血。将题目类型（web/pwn/reverse/misc/crypto）作为唯一的 tag，不设其他 tag。